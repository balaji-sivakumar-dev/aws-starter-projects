# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
.env

# Node / React
node_modules/
dist/
.env.local

# Docker
.env           # real secrets — never commit

# User access config (copy from .env.users.example, never commit)
.env.users

# Terraform
.terraform/
*.tfstate
*.tfstate.backup
*.tfvars
!*.tfvars.example
.terraform.lock.hcl

# Lambda build artifacts (regenerated on every deploy)
**/.build/

# Local test scripts with real credentials (copy from *.example, never commit)
*.local
scripts/test_groq_local.sh

# macOS
.DS_Store
