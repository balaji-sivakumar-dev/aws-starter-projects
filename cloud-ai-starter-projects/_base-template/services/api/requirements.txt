# Core API
fastapi==0.115.6
uvicorn[standard]==0.32.1
boto3==1.35.93
python-jose[cryptography]==3.3.0
httpx==0.27.2
pydantic==2.10.3

# LLM providers (lazy-imported — only used when LLM_PROVIDER env var is set)
ollama==0.4.4
groq==0.13.0
openai==1.59.3

# RAG — vector store (lazy-imported — only used when VECTOR_STORE env var is set)
chromadb==0.6.3
